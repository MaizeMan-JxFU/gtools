[build-system]
requires = ["maturin>=1.6"]
build-backend = "maturin"

[project]
name = "jxglm-rs"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["numpy"]

[tool.maturin]
module-name = "jxglm_rs"
bindings = "pyo3"